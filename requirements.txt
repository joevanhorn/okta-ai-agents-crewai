# Dependencies for the Account Risk Monitor. These mirror pyproject.toml.
# Prefer `./setup.sh` (or `pip install -e .`), which uses pyproject directly;
# this file is here for `pip install -r requirements.txt` workflows.
crewai>=1.14.6,<2.0
crewai-tools[mcp]>=1.14.6,<2.0
requests>=2.31,<3.0
python-dotenv>=1.0,<2.0
PyJWT[crypto]>=2.8,<3.0
boto3>=1.34
